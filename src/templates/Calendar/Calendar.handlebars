<div class="calendar">
    <div class="calendar-header">
        <button class="arrow-button" id="prevMonthBtn">
            <img src="/assets/img/arrow-icon.svg" />
        </button>
        <div class="calendar-header__month-container">
            <span class="calendar-header__month">{{month}}</span>
            <span class="calendar-header__year">{{year}}</span>
        </div>
        <button class="arrow-button">
            <img src="/assets/img/arrow-icon.svg" class="reversed" id="nextMonthBtn" />
        </button>
    </div>
    <table>
        <tr class="top-row">
            <th>ПН</th>
            <th>ВТ</th>
            <th>СР</th>
            <th>ЧТ</th>
            <th>ПТ</th>
            <th>СБ</th>
            <th>ВС</th>
        </tr>
        {{#each weeks}}
            <tr>
                {{#each this}}
                    <td><button class="calendar-date current {{style}}">{{this.date}}</button></td>
                {{/each}}
            </tr>
        {{/each}}
    </table>
</div>