{
  "type": "module",
  "dependencies": {
    "debug": "^4.3.4",
    "handlebars": "^4.7.7"
  },
  "scripts": {
    "start": "echo 'export default {HOST:\"http://95.163.213.222/api\"}' > ./public/config.js & node server",
    "dev": "echo 'export default {HOST:\"http://localhost:8000\"}' > ./public/config.js & node server",
    "devwin": "echo export default {HOST:\"http://localhost:8000\"} > ./public/config.js & node server",
    "prestart": "npm run build:templates",
    "predev": "npm run build:templates",
    "predevwin": "npm run build:templates",
    "build:templates": "node server/componentInit.js"
  },
  "devDependencies": {
    "eslint": "^8.35.0"
  }
}
